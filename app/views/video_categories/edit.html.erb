<h2><%= t('.title') %></h2>

<%= render "shared/errors", resource: @video_category %>

<%= render 'form' %>

<div><%= button_to 'Cancelar categoria', cancel_video_category_path(@video_category), method: :post, class: "btn btn-outline-primary mt-3 mb-3" %></div>
<div><%= link_to t('links.go_back'), :back, class: "btn btn-outline-primary mt-3 mb-3" %></div>
